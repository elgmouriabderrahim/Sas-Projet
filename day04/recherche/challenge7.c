#include <stdio.h>


struct list{
	int  val;
	struct list *svt;
};
typedef struct list list;
int rechercheD(int tab[],int  nb){

}
int main(){
	list *List = NULL;
	list
	do{
		printf("entrer 1 pour ajouter un valeur");
		scanf("%d",)
	}
	if(rechercheB())
		printf("l'indexe du %s est %d\n", );
 	else
		printf("l'element n'est pas exist\n", );
	return 0;
}
